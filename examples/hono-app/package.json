{
  "name": "hono-app",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=.env.local --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "preview": "bun dist/index.js"
  },
  "dependencies": {
    "hono": "^4.9.8",
    "@kaanmertkoc/iyzico-subs-ts": "file:../..",
    "zod": "^3.22.4",
    "@hono/zod-validator": "^0.2.2"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}